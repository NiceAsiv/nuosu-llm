# Patches / 事故修复

此目录保存针对某次事故或特定运行状态的一次性处置脚本。补丁有保留价值，但不属于正常
安装、训练或评测流程。

规则：

- 按 `YYYY-MM-DD/` 归档；
- 每个目录必须说明触发条件、影响范围、使用前置条件和是否会移动数据；
- 补丁不能成为主 README 的默认入口；
- 可复用的能力应提炼到 `src/` 或稳定的 `scripts/`，而不是继续堆叠补丁；
- 已经完成使命的补丁保留用于审计，不继续增加兼容分支。

当前归档：

- [`2026-07-29/`](2026-07-29/)：损坏模型缓存恢复与三卡流水线中断恢复。
